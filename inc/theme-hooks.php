<?php
/**
 * Theme hook stub list.
 *
 * @package  CsStarter
 * @subpackage csstarter/inc
 * @author   Chip Sheppard
 * @since    1.0.0
 * @license    GPL-2.0+
 */

/**
 * HTML <html> hook
 */
function csstarter_html_before() {
	do_action( 'csstarter_html_before' );
}

/** HTML <body> hooks */
function csstarter_body_top() {
	do_action( 'csstarter_body_top' );
}

/** Hook */
function csstarter_body_bottom() {
	do_action( 'csstarter_body_bottom' );
}

/**
 * HTML <head> hooks
 */
function csstarter_head_top() {
	do_action( 'csstarter_head_top' );
}

/** Hook */
function csstarter_head_bottom() {
	do_action( 'csstarter_head_bottom' );
}

/**
 * Semantic <header> hooks
 */
function csstarter_header_before() {
	do_action( 'csstarter_header_before' );
}

/** Hook */
function csstarter_header_after() {
	do_action( 'csstarter_header_after' );
}

/** Hook */
function csstarter_header_top() {
	do_action( 'csstarter_header_top' );
}

/** Hook */
function csstarter_header_bottom() {
	do_action( 'csstarter_header_bottom' );
}

/** Hook */
function csstarter_header_before_wrap() {
	do_action( 'csstarter_header_before_wrap' );
}

/** Hook */
function csstarter_header_after_wrap() {
	do_action( 'csstarter_header_after_wrap' );
}

/**
 * Semantic <content> hooks
 */
function csstarter_content_before() {
	do_action( 'csstarter_content_before' );
}

/** Hook */
function csstarter_content_wrap_before() {
	do_action( 'csstarter_content_wrap_before' );
}

/** Hook */
function csstarter_content_loop() {
	do_action( 'csstarter_content_loop' );
}

/** Hook */
function csstarter_content_wrap_after() {
	do_action( 'csstarter_content_wrap_after' );
}

/** Hook */
function csstarter_content_after() {
	do_action( 'csstarter_content_after' );
}

/** Hook */
function csstarter_content_top() {
	do_action( 'csstarter_content_top' );
}

/** Hook */
function csstarter_content_bottom() {
	do_action( 'csstarter_content_bottom' );
}

/** Hook */
function csstarter_content_while_before() {
	do_action( 'csstarter_content_while_before' );
}

/** Hook */
function csstarter_content_while_after() {
	do_action( 'csstarter_content_while_after' );
}

/**
 * Semantic <entry> hooks
 */
function csstarter_entry_before() {
	do_action( 'csstarter_entry_before' );
}

/** Hook */
function csstarter_entry_after() {
	do_action( 'csstarter_entry_after' );
}

/** Hook */
function csstarter_entry_content_before() {
	do_action( 'csstarter_entry_content_before' );
}

/** Hook */
function csstarter_entry_content_after() {
	do_action( 'csstarter_entry_content_after' );
}

/** Hook */
function csstarter_entry_top() {
	do_action( 'csstarter_entry_top' );
}

/** Hook */
function csstarter_entry_bottom() {
	do_action( 'csstarter_entry_bottom' );
}

/**
 * Comments block hooks
 */
function csstarter_comments_before() {
	do_action( 'csstarter_comments_before' );
}

/** Hook */
function csstarter_comments_after() {
	do_action( 'csstarter_comments_after' );
}

/**
 * Semantic <sidebar> hooks
 */
function csstarter_sidebars_before() {
	do_action( 'csstarter_sidebars_before' );
}

/** Hook */
function csstarter_sidebars_after() {
	do_action( 'csstarter_sidebars_after' );
}

/** Hook */
function csstarter_sidebar_top() {
	do_action( 'csstarter_sidebar_top' );
}

/** Hook */
function csstarter_sidebar_bottom() {
	do_action( 'csstarter_sidebar_bottom' );
}

/**
 * Semantic <footer> hooks
 */
function csstarter_footer_before() {
	do_action( 'csstarter_footer_before' );
}

/** Hook */
function csstarter_footer_after() {
	do_action( 'csstarter_footer_after' );
}

/** Hook */
function csstarter_footer_top() {
	do_action( 'csstarter_footer_top' );
}

/** Hook */
function csstarter_footer_bottom() {
	do_action( 'csstarter_footer_bottom' );
}
